import React from 'react'
import Article from './Article'

const ArticleList = () => {

  return (
    <div>
      {/* {
        array.map(item => <Article />)
      } */}
    </div>
  )
}

export default ArticleList
